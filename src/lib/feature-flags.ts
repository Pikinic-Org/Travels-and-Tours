// Temporary kill-switches for forms/flows that aren't ready for public
// traffic yet. Flip a flag back to `true` once that flow is ready —
// nothing else needs to change.
export const featureFlags = {
  comments: false,
  customPackageRequest: false,
};
